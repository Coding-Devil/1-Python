num = input("Enter the number: " )
# we denote exponential powers by double **
sqr_root = float(num) ** 0.5
# for squares we raise the power to 2
num_sqr_rt = f"The square root of {num} is {sqr_root}"
print(num_sqr_rt)